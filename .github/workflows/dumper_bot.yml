name: Run command via SSH every day or on workflow dispatch
on:
  schedule:
    - cron: '0 0 * * *' # runs at 12:00 AM UTC every day
  workflow_dispatch:

jobs:
  run-command:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            script.sh
      - name: Reboot Server
        run: |
          sudo apt update
          sudo apt-get install expect -y
          bash restart.sh ${{ secrets.SEGFAULT_TOKEN }}
      - name: Start Server
        run: |
          bash script.sh ${{ secrets.SEGFAULT_TOKEN }}